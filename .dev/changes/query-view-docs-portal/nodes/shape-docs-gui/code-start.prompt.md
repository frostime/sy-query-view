阅读本目录的 `TASK-NODE.SPEC.md` 并完成该设计任务。`../define-doc-structure/DOC-STRUCTURE.md` 是已验收的内容契约，`../write-human-docs/TASK-NODE.SPEC.md` 说明已完成的 docs 产物。

只写入本节点目录，不修改任何生产文件或全局 change 文件。给出能约束后续实现的最小代码形状，而不是罗列理想化组件。发现需要改变用户行为、范围、内容来源或关键架构的冲突时立即报告；局部实现选择直接给出推荐。完成后更新任务规格状态和结果。