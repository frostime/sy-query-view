阅读本目录 `TASK-NODE.SPEC.md` 及其列出的已验收资料后执行。必须把“旧帮助笔记机制”作为一个整体退役：先确保 Help 已指向文档站，再同时移除旧消费者、设置和 README Reference 标记；绝不操作或删除用户知识库中的现有笔记。

严格只修改允许路径。保留 `dts-actions.ts`、文档站和基础模板，避免清理 `src/api.ts` 等当前任务外的通用 API。若实际依赖与任务规格冲突，或必须触及用户数据/禁止路径，立即停止报告；其他局部问题自行决定并记录。完成后运行验证、更新任务规格为“等待验收”并报告。