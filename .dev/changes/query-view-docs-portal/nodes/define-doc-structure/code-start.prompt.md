阅读本目录的 `TASK-NODE.SPEC.md`，按照其中要求完成任务。

预设其中给定的目标、边界和前置决策均正确，不做与交付物无关的过度调研。保持独立判断：若仓库证据与任务规格冲突，或必须改变用户行为、内容来源、范围、关键架构，立即暂停并反馈；局部细节自行决定并在交付物中说明。

只写入本节点目录，不修改生产代码或全局 change 文件。完成后把 `TASK-NODE.SPEC.md` 状态改为“等待验收”，填写“结果与影响”，并报告查阅的关键文件。