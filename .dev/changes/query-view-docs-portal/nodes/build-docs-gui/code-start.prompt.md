阅读本目录的 `TASK-NODE.SPEC.md`，尤其严格遵守 `../shape-docs-gui/docs-site.LAND.md`。它是本任务的代码结构与行为契约；不要重新设计模块边界或省略其中的竞态、回退、只读和依赖叶规则。

只修改任务规格明确允许的路径。不要删除旧帮助/Examples/设置，也不要修改 docs 或构建系统。发现必须改变用户行为、范围、内容来源、LAND 的关键契约或无法满足验收条件的事实时，停止并报告；其余局部细节自行选择并写入本节点结果或 `DECISIONS.md` 建议。完成后运行验证、更新任务规格为“等待验收”并报告。