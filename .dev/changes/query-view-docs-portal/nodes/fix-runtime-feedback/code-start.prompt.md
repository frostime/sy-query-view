阅读本目录 `TASK-NODE.SPEC.md` 及其列出的当前文件和用户截图 DOM 证据后执行。只修改允许路径，不提交，不连接真实 SiYuan 用户数据。

实现重点：站内语言只跟随 SiYuan；用 DOM 删除覆盖图片与代码块的 Lute 原生编辑控件，再注入文档站复制按钮；概念图改为中英文共用本地 SVG。你的模型没有视觉能力，不得声称截图或 SVG 视觉验收通过，必须把视觉复核留给主 Agent。完成后运行可复现验证、恢复 `public/types.d.ts` 构建副作用并更新任务状态。