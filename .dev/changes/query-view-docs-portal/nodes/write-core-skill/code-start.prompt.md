阅读本目录的 `TASK-NODE.SPEC.md` 和其中列出的权威材料。编写一个短而可执行的英文核心 Skill，不要把 README 全文搬进去，不要假定 `references/` 可读，也不要修改 docs、GUI、打包或 Agent 集成代码。

所有 API 和示例必须能追溯到现有 types、基础模板或案例。若发现需要改变用户行为、技能加载模型、语言决定或范围，立即停止报告；其余措辞和组织细节自行判断。仅写允许路径，完成后验证并更新任务规格。