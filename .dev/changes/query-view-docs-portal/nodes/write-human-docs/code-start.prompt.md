阅读本目录的 `TASK-NODE.SPEC.md`，按照其中要求完成任务。

`../define-doc-structure/DOC-STRUCTURE.md` 是已验收的页面与内容契约。严格保持 docs 为人类文档唯一手工来源、案例代码在 `public/example/` 唯一维护、README 自动生成的边界。不要修改 GUI 或旧帮助机制。

只修改任务规格允许的路径，不修改全局 change 文件、`.gitignore` 或 git 历史。发现会改变用户行为、内容来源、任务范围、关键架构或无法满足验收条件的事实时，立即停止并报告；其他局部细节自行决定，并在本任务结果中说明。完成后运行规定验证，更新 `TASK-NODE.SPEC.md` 状态为“等待验收”并填写结果。