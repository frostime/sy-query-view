# 小型实现决定

本文件只记录执行过程中不会改变用户行为、内容来源、任务范围或关键架构，但值得让后续执行者知道的局部决定。原则性变化应先询问用户，并更新 `TARGET.SPEC.md` 与 `graph.md`。

每项简要记录：

```markdown
## YYYY-MM-DD — <决定>

- 任务：<task slug>
- 决定：<采用什么>
- 原因：<为什么>
- 影响：<涉及哪些局部文件或行为>
```

当前没有实现决定。
